> [!NOTE]
> 便捷简单的文档数据库，始终如一。

`MisakaDB` 是一个轻量级的JSON文档数据库，支持`JSON`格式的内容字段存储，在0.0.1版本，仅支持单机模式。

## 启动服务

### 源码启动

服务端的代码由GO语言编写，需要安装go语言服务后，执行命令：

```python
go run misaka.go [参数] 
```

### 程序启动

程序支持多平台


在0.0.1版本，支持如下参数启动：

- **address**    启动的地址簇
- **port**            启动的端口